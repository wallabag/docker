sudo: false

language: php

php:
  - 7.0

services:
  - docker

install:
  - docker build -t wallabag .
  - docker run -d -p 127.0.0.1:80:80 --name wallabag wallabag

script:
  - docker ps | grep -q wallabag
